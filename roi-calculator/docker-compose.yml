services:
  backend:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app/api
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - ENVIRONMENT=development
